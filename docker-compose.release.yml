# Set MPT_IMAGE explicitly to an image built from this repository. Prefer the
# publication digest: ghcr.io/<owner>/<repo>@sha256:<digest>.
x-common-volumes: &common-volumes
  - type: bind
    source: ./config.toml
    target: /MoneyPrinterTurbo/config.toml
    bind:
      create_host_path: false
  - ./storage:/MoneyPrinterTurbo/storage

services:
  webui:
    platform: linux/amd64
    image: "${MPT_IMAGE:?Set MPT_IMAGE to your published image digest or full commit tag}"
    container_name: "moneyprinterturbo-webui"
    ports:
      - "127.0.0.1:8501:8501"
    command: [ "streamlit", "run", "./webui/Main.py", "--server.address=0.0.0.0", "--server.port=8501", "--browser.serverAddress=127.0.0.1", "--server.enableCORS=True", "--browser.gatherUsageStats=False", "--client.toolbarMode=minimal", "--logger.hideWelcomeMessage=True", "--server.showEmailPrompt=False" ]
    volumes: *common-volumes
    restart: always
  api:
    platform: linux/amd64
    image: "${MPT_IMAGE:?Set MPT_IMAGE to your published image digest or full commit tag}"
    container_name: "moneyprinterturbo-api"
    ports:
      - "127.0.0.1:8080:8080"
    command: [ "python3", "main.py" ]
    volumes: *common-volumes
    restart: always
